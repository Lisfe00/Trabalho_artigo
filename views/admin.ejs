<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/css/index.css">
    <title>Document</title>
</head>
<body>
    <header id="header">
        <img class="image" src="../public/img/compilando_comnhecimento.png" width="15%">
        <div class="as"> 
            <a class="link" href="/articles/show/create">Cadastrar artigo</a>
            <a class="link" <% if (authUser == "admin") { %> href="/users/show/create" <% }else { %> href="/users/home" <% } %>>Cadastrar usuário</a>
        </div>
        <a class="link" href="/users/logout">logout</a>
    </header>
    <main id="main">
        <div class="title">
            Que bom ter você aqui, <br><%= authName %>!
            <div class="subtitle">
                Como administrador você pode gerenciar os <br>artigos e usuários!
            </div>
        </div>

        <label class="lable-artigos">Artigos</label>

        <div class="artigos_container">
            <% if (Array.isArray(datasArticles)) { %>
                <% datasArticles.forEach(function(dataArticle) { %>
                    <div class="card-articles">
                        <div class="admin-card-title">
                            <a class="titulo-admin" href="/articles/show/update/<%= dataArticle.kb_id%>" ><%= dataArticle.kb_title %></a>
                            <br>
                            <span class="autor"><%= dataArticle.author_name %></span>
                        </div>
                        <img class="garbage-img" src="../public/img/garbage.svg" alt="" width="2%">
                    </div>
                <% }); %>
            <% } else { %>
                <label class="serch_label">Não há artigos</label>
            <% } %>      
        </div>

        <br>
    
    <% if (authUser == "admin") { %>
        <label class="lable-artigos">Usuários</label>

        <div class="artigos_container">
            <% if (Array.isArray(datas)) { %>
                <% datas.forEach(function(data) { %>
                    <div class="card-users">
                        <div class="admin-card-title">
                            <a class="titulo-admin" href="/users/show/update/<%= data.author_id%>"><%= data.author_name %></a>
                            <br>
                            <span class="autor"><%= data.author_email %></span>
                        </div>
                        <img class="garbage-img" src="../public/img/garbage.svg" alt="" width="2%">
                    </div>
                <% }); %>
            <% } else { %>
                <label class="serch_label">Não há artigos</label>
            <% } %>      
        </div>
        <% } %>
    </main>


    <!-- <% if (authUser == "admin") { %>
    <table>
        <thead>
            <td>Nome</td>
            <td>E-mail</td>
            <td>Usuário</td>
            <td>Permimssão</td>
            <td>Ativo</td>
        </thead>
        <tbody>
            <% if (Array.isArray(datas)) { %>
                <% datas.forEach(function(data) { %>
                    <tr>
                        <td><%= data.author_name %></td>
                        <td><%= data.author_email %></td>
                        <td><%= data.author_user %></td>
                        <td><%= data.author_level %></td>
                        <td><%= data.author_status %></td>
                        <td><a href="/users/show/update/<%= data.author_id%>">Editar</a></td>
                        <td><a href="/users/delete/<%= data.author_id%>">Deletar</a></td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td>Data is not an array.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
    <% } %> -->

</body>
</html>